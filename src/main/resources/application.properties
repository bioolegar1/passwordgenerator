spring.datasource.url=${SPRING_DATASOURCE_URL} # Usaremos esta vari�vel de ambiente no Render
spring.datasource.username=${DB_USER} # Opcional se j� estiver na URL
spring.datasource.password=${DB_PASSWORD} # Opcional se j� estiver na URL

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.data-source-properties.sslmode=require